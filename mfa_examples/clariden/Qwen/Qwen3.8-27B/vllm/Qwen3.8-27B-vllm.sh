#!/usr/bin/env bash
# Generated by model-launch-factory run a18b4e4c672142b69e30f7c298f555db on 2026-09-22
# Gate: probe + streaming passed; advisory: core 11/13 (core_multi_system, core_assistant_prefill); multimodal 0/8 (mm_image_small, mm_image_large, mm_image_multi, mm_audio_small, mm_audio_large, mm_interleaved, mm_no_degeneration_hard, mm_no_degeneration_hard_audio)
# Full mcs report, evals and benchmarks: see mcs/, evals/, benchmarks/ next to this file.
#
# Once the catalog entry from this PR is merged, the same launch is one line:
#   sml preconfigured --model Qwen/Qwen3.8-27B --framework vllm \
#     --system clariden --partition preemptable --time 03:00:00
# Below is the exact `sml advanced` form the factory verified: `sml advanced` has no
# --model flag; the weights come from the capstor registry via --framework-args.
set -euo pipefail
sml advanced \
  --system clariden \
  --partition preemptable \
  --framework vllm \
  --served-model-name Qwen/Qwen3.8-27B-vllm \
  --replicas 1 \
  --nodes-per-replica 1 \
  --time 03:00:00 \
  --environment src/swiss_ai_model_launch/assets/envs/vllm.toml \
  --framework-args "--model /capstor/store/cscs/swissai/infra01/hf_models/models/Qwen/Qwen3.8-27B --tensor-parallel-size 4 --max-model-len 131072 --max-num-seqs 16 --gpu-memory-utilization 0.85 --reasoning-parser qwen3"
